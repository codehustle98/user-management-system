group = 'org.codehustle'
version = '1.0.0'

repositories {
    mavenCentral()
}

allprojects {
    apply {
        plugin 'java'
    }
}

subprojects {
    sourceCompatibility=17
    targetCompatibility=17
}